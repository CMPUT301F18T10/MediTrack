/*package com.example.meditrack;

import org.junit.Test;

import static junit.framework.TestCase.assertTrue;
import static org.junit.Assert.assertFalse;

public class RegistrationTest {

    /*
    Example

    @Test
    public void testRegistration(){

        pseudo-code for this test:

        String userId = "tester";
        ApplicationManager applicationManager = new ApplicationManager();
        SomeOtherManager mgr = new SomeOtherManager();

        // We expect the first registration to go through
        assertTrue(applicationManager.RegisterUser(userId, ApplicationManager.UserMode.CareGiver));

        // Now, we search our database and verify that we can find the registered user
        assertTrue(mgr.userExist(userID, ApplicationManager.UserMode.CareGiver));
    }


    @Test
    public void testDoubleRegistration(){
        String userId = "tester";
        ApplicationManager applicationManager = new ApplicationManager();

        // We expect the first registration to go through
        assertTrue(applicationManager.RegisterUser(userId, ApplicationManager.UserMode.CareGiver));

        // We expect the registration to fail when we try to register with the same Id
        assertFalse(applicationManager.RegisterUser(userId, ApplicationManager.UserMode.CareGiver));
    }

}
*/
